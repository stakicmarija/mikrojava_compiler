program Oporavak

int x y;
int a, , b;

{
    int sum(int p, , int q) {
        return p + q;
    }

    void main() int i; {

        x = ;
        a = 5 + ;

        if (x > )
            print(x);

        if (x > 5 && )
            print(x);

        for(i = 0; i < ; i++)
        {
            print(i);
        }

        print(10);
    }
}
